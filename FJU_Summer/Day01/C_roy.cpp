#include <iostream>

using namespace std;

int main()
{
	#ifdef DBG
	freopen("C_r.in", "r", stdin);
	freopen("C_r.out", "w", stdout);
	#endif
	int beam, recv, tmp;

	scanf("%d %d %d %d", &beam, &recv, &tmp, &tmp);

	

	return 0;
}